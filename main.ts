input.onButtonPressed(Button.A, function () {
	
})
basic.clearScreen()
let encendido = 0
basic.forever(function () {
    if (true) {
        basic.showIcon(IconNames.Heart)
    } else {
        basic.clearScreen()
    }
})
